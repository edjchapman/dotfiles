# Pinned for reproducibility. Bump via Dependabot (pip group).
# Keep alphabetised so `sort -c` passes the pre-commit hook.
# mkdocs-macros-plugin omitted — its Jinja2 `{{ }}` syntax collides with
# chezmoi templates that the docs frequently reference verbatim.
mkdocs-git-revision-date-localized-plugin==1.5.4
mkdocs-glightbox==0.5.2
mkdocs-include-markdown-plugin==7.3.0
mkdocs-material[imaging]==9.7.7
mkdocs-minify-plugin==0.8.0
mkdocs-redirects==1.2.3
mkdocs-rss-plugin==1.19.0
mkdocs-section-index==0.3.12
pymdown-extensions==11.0.2
