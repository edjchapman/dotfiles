# Homebrew Bundle — managed by chezmoi
# Applied automatically by chezmoi when this file changes
# To add packages: edit this file, then run `chezmoi apply`
#
# Entry lines must NOT carry trailing comments — put comments on their own
# line. The sync tooling (chezmoi-brew-sync) matches entries with
# line-anchored patterns: a trailing comment such as `cask "foo"  # pinned`
# makes the entry invisible to it (duplicates on re-install, silent no-op
# on removal).

# Taps
tap "hashicorp/tap"

# CLI Tools
brew "bash"
brew "bat"
brew "chezmoi"
brew "coreutils"
brew "curl"
brew "diffutils"
brew "dockutil"
brew "dust"
brew "eza"
brew "fd"
brew "findutils"
brew "fnm"
brew "fzf"
brew "gawk"
brew "gh"
brew "git"
brew "git-filter-repo"
brew "gnu-sed"
brew "go"
brew "graphviz"
brew "grep"
brew "opentofu"
brew "htop"
brew "jq"
brew "less"
brew "lychee"
brew "mas"
brew "nmap"
brew "pandoc"
brew "pipx"
brew "poppler"
brew "pre-commit"
brew "python@3.13"
brew "rclone"
brew "restic"
brew "ripgrep"
brew "rsync"
brew "ruby"
{{ if eq .machine_type "personal" -}}
brew "sherlock"
{{ end -}}
brew "hashicorp/tap/terraform"
brew "tlrc"
brew "tree"
brew "valkey"
brew "vhs"
brew "watch"
brew "wget"
brew "zsh"
brew "zsh-completions"

# Build / Dev dependencies
brew "openjdk@17"
brew "openssl@3"
brew "pkgconf"

# Security & Privacy
brew "age"
brew "awscli"
brew "ggshield"
brew "gitleaks"

# Linters / formatters used by this repo's CI and pre-commit
brew "shellcheck"
brew "shfmt"
brew "yamllint"
brew "markdownlint-cli2"

# Test framework for tests/ (chezmoi-fix menu rendering, drift-check summary)
brew "bats-core"

# Desktop Apps — Browsers
cask "brave-browser"
cask "google-chrome"
cask "tor-browser"

# Desktop Apps — Dev Tools
cask "claude"
cask "claude-code"
cask "iterm2"
cask "jetbrains-toolbox"
cask "orbstack"
cask "visual-studio-code"

# Desktop Apps — Productivity
cask "anki"
cask "google-drive"
cask "microsoft-excel"
cask "microsoft-word"

# Desktop Apps — Utilities
cask "bluesnooze"

# Desktop Apps — Privacy & Security
cask "fing"
cask "gpg-suite"
cask "lulu"
cask "nordvpn"
cask "proton-mail"

# Desktop Apps — AI
cask "chatgpt"

{{ if eq .machine_type "personal" -}}
# Dev Tools — CLI, but distributed as a cask (migrated from homebrew/core)
cask "android-platform-tools"

# Desktop Apps — Media & Social
cask "tidal"
cask "whatsapp"

# Desktop Apps — Crypto
cask "monero-wallet"
cask "sparrow"

# Desktop Apps — Mail
cask "tuta-mail"
{{ end -}}

# Mac App Store — requires a signed-in Apple ID. On a fresh machine, sign in to
# the App Store first, otherwise `brew bundle` (run_onchange_02) exits non-zero.
mas "Amphetamine", id: 937984704
